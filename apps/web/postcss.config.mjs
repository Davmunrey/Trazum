/**
 * Tailwind v4 needs no config file: the theme lives in `app/globals.css`
 * behind `@theme inline`, next to the palette it derives from.
 */
const config = {
  plugins: { '@tailwindcss/postcss': {} },
};

export default config;
