{
  "name": "trazum",
  "version": "0.10.0",
  "private": true,
  "description": "Prompt optimiser: cut the cost of your AI calls without losing quality.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Davmunrey/Trazum.git"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @trazum/core && npm run build -w @trazum/cli",
    "test": "npm run test -w @trazum/core && npm run test -w @trazum/cli",
    "dev:web": "npm run dev -w @trazum/web",
    "build:web": "npm run build -w @trazum/web",
    "trazum": "node packages/cli/dist/index.js"
  }
}
