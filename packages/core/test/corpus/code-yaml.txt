You are a deployment assistant. Given this manifest, say what will not start.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: writer
  labels: { app: writer, tier: api }
spec:
  replicas: 3
  selector:
    matchLabels: { app: writer }
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
        - name: writer
          image: registry.internal/writer@sha256:9f2c1a
          ports: [{ containerPort: 8080 }, { containerPort: 9090 }]
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: writer-db, key: url } }
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "250m", memory: "192Mi" }
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 1
```

Answer with BLOCKING and WARNING. One line each, no rewritten YAML.
